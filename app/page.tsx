"use client";

import { useEffect } from "react";
import { useRouter, usePathname } from "next/navigation";

export default function Home() {
  // El AuthProvider en layout.tsx se encarga de las redirecciones automáticas
  // dependiendo del estado de la sesión y la ruta.
  return null;
}
